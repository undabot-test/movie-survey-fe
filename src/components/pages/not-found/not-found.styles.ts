import { Container, styled } from '@mui/material'

export const Page = styled(Container)({
  marginTop: 128,
  marginBottom: 128,
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
})
